# 下个项目优化指南：与当前项目对齐

最后更新：2026-05-20

## 一、目标

将「信息分析与知识沉淀」项目（analyse_information）与当前「信息获取」项目（search_information/TrendRadar）完全对齐，实现：

```
TrendRadar（信号采集） → n8n（自动化） → analyse_information（AI分析） → Obsidian（知识沉淀）
```

---

## 二、修改清单与完成状态

| 序号 | 文件 | 修改内容 | 优先级 | 状态 |
|---|---|---|---|---|
| 1 | analyzer/config.yaml | RSS源对齐（12个源） | 高 | ✅ 已完成 |
| 2 | analyzer/config.yaml | 关键词对齐（金字塔模型，6组） | 高 | ✅ 已完成 |
| 3 | analyzer/config.yaml | 分类规则优化（10个分类，带权重） | 高 | ✅ 已完成 |
| 4 | .env | 添加 DINGTALK_WEBHOOK | 中 | ⏳ 待完成 |
| 5 | n8n/workflows/信息分析知识沉淀.json | 工作流整合（接收TrendRadar信号） | 中 | ✅ 已完成 |
| 6 | analyzer/rss_fetcher.py | 支持从外部项目读取RSS源 | 中 | ✅ 已完成 |
| 7 | analyzer/ai_analyzer.py | 金字塔模型权重集成 | 低 | ✅ 已完成 |
| 8 | Obsidian目录 | 创建10个新分类目录 | 中 | ✅ 已完成 |

---

## 三、已完成的修改详情

### 3.1 ✅ config.yaml — RSS源对齐

**实际配置（12个RSS源）**：
```yaml
rss:
  enabled: true
  feeds:
    - name: "LWN.net"
      url: "https://lwn.net/headlines/newrss"
    - name: "Phoronix"
      url: "https://www.phoronix.com/rss.php"
    - name: "Linux Kernel Mailing List"
      url: "https://lore.kernel.org/lkml/?q=d:1.day.ago&x=A"
    - name: "CNX Software"
      url: "https://www.cnx-software.com/feed/"
    - name: "Linux Weekly News"
      url: "https://www.linux.com/rss/"
    - name: "Kernel.org"
      url: "https://www.kernel.org/feeds/all.atom.xml"
    - name: "Linux Journal"
      url: "https://www.linuxjournal.com/node/feed"
    - name: "Linux Gizmos"
      url: "https://linuxgizmos.com/feed/"
    - name: "Hackaday"
      url: "https://hackaday.com/feed/"
    - name: "Embedded.com"
      url: "https://www.embedded.com/feed/"
    - name: "EE Times"
      url: "https://www.eetimes.com/feed/"
    - name: "The Register"
      url: "https://www.theregister.com/headlines.atom"
```

同时配置了 `rss_source` 支持从外部项目读取：
```yaml
rss_source: "C:/Users/zhuxiangbo/Desktop/project/search_information/trendradar/config.yaml"
```

---

### 3.2 ✅ config.yaml — 金字塔模型关键词（6组）

**实际配置**：
```yaml
relevance:
  keywords:
    # 第1层：求职招聘（最高优先级）
    job_hunting:
      - "嵌入式Linux实习"
      - "C++开发实习"
      - "2026校招提前批"
      - "嵌入式校招"
      - "BSP工程师"
      - "实习僧"
      - "Boss直聘"

    # 第2层：面经与题库
    interview:
      - "嵌入式面经2026"
      - "C++面经2026"
      - "Linux系统编程面试题"
      - "手撕代码嵌入式"

    # 第3层：政策与福利
    policy:
      - "应届生租房补贴"
      - "大学生就业补贴江西"
      - "学生云服务器优惠"
      - "GitHub Student Pack"

    # 第4层：行业动态
    industry:
      - "嵌入式行业新闻"
      - "Linux内核新版本"
      - "RISC-V进展"
      - "Linux内核"

    # 第5层：长期杠杆
    leverage:
      - "开源之夏2026"
      - "Google Summer of Code"
      - "嵌入式竞赛2026"
      - "景德镇人才引进2026"

    # 第6层：技术关键词（补充）
    tech:
      - "嵌入式"
      - "BSP"
      - "驱动"
      - "RISC-V"
      - "IoT"
      - "设备树"
      - "U-Boot"
      - "交叉编译"
      - "Yocto"
      - "Buildroot"
      - "GPIO"
      - "I2C"
      - "SPI"
      - "UART"
      - "RTOS"
      - "FreeRTOS"
      - "Zephyr"

  min_score: 6
```

---

### 3.3 ✅ config.yaml — 分类规则（10个分类，带权重）

**实际配置**：
```yaml
categories:
  - name: "01-求职招聘"
    keywords: ["实习", "校招", "招聘", "BSP工程师", "嵌入式工程师", "简历", "面试"]
    weight: 1.5

  - name: "02-面经题库"
    keywords: ["面经", "面试题", "笔试", "手撕代码", "LeetCode", "八股文"]
    weight: 1.2

  - name: "03-政策福利"
    keywords: ["补贴", "优惠", "学生", "GitHub Student", "GSoC", "人才引进"]
    weight: 1.0

  - name: "04-行业动态"
    keywords: ["行业", "新闻", "趋势", "公司", "大疆", "联发科", "Qualcomm", "NVIDIA"]
    weight: 0.8

  - name: "05-长期杠杆"
    keywords: ["开源之夏", "竞赛", "Coursera", "学习", "成长"]
    weight: 0.6

  - name: "06-嵌入式Linux"
    keywords: ["Linux", "内核", "Kernel", "Buildroot", "Yocto", "根文件系统", "交叉编译"]
    weight: 0.5

  - name: "07-BSP开发"
    keywords: ["BSP", "板级支持包", "U-Boot", "设备树", "Device Tree", "启动流程"]
    weight: 0.5

  - name: "08-设备驱动"
    keywords: ["驱动", "Driver", "GPIO", "I2C", "SPI", "UART", "中断", "DMA", "字符设备"]
    weight: 0.5

  - name: "09-RISC-V"
    keywords: ["RISC-V", "riscv", "SiFive", "平头哥", "玄铁"]
    weight: 0.5

  - name: "10-IoT"
    keywords: ["IoT", "物联网", "MQTT", "传感器", "RTOS", "FreeRTOS", "Zephyr"]
    weight: 0.5
```

---

### 3.4 ✅ rss_fetcher.py — 支持外部RSS源

已修改 `load_rss_feeds()` 函数，支持从 `rss_source` 配置的外部路径读取：

```python
def load_rss_feeds() -> list:
    # 优先从 rss_source 指定的外部配置读取
    # 如果外部配置不存在或读取失败，回退到本地 rss.feeds
```

---

### 3.5 ✅ ai_analyzer.py — 金字塔模型权重

已添加两个新方法：

- `_flatten_keywords(keywords_config)` — 将嵌套的关键词字典展平为列表
- `_calculate_weighted_score(content, categories)` — 基于分类权重计算加权评分

分析流程：
1. AI 给出基础评分
2. 关键词匹配计算加权评分
3. 取两者中的较高值作为最终评分

---

### 3.6 ✅ n8n工作流 — 接收TrendRadar信号

工作流节点已更新：
```
1. 接收TrendRadar信号（Webhook）
   ↓
2. 运行AI分析Pipeline
   ↓
3. 是否相关(score>=6)
   ├─ 否 → 返回跳过
   └─ 是 → 钉钉通知 + 返回分析结果
```

---

### 3.7 ✅ Obsidian目录 — 10个新分类

已创建的目录：
```
knowledge_base/obsidian/
├── 00-收件箱/
├── 01-求职招聘/     ← 新增
├── 02-面经题库/     ← 新增
├── 03-政策福利/     ← 新增
├── 04-行业动态/     ← 新增
├── 05-长期杠杆/     ← 新增
├── 06-嵌入式Linux/  ← 新增
├── 07-BSP开发/      ← 新增
├── 08-设备驱动/     ← 新增
├── 09-RISC-V/       ← 新增
├── 10-IoT/          ← 新增
└── templates/
```

---

## 四、待完成的修改

### 4.1 ⏳ .env — 添加钉钉Webhook

**当前状态**：
```
DEEPSEEK_API_KEY=your-deepseek-api-key
ANALYZER_PATH=C:\Users\zhuxiangbo\Desktop\project\analyse_information
```

**需要添加**：
```
DINGTALK_WEBHOOK=你的钉钉Webhook地址
```

**操作步骤**：
1. 登录钉钉，创建/找到群机器人
2. 复制 Webhook 地址
3. 打开 `analyse_information/.env`
4. 添加 `DINGTALK_WEBHOOK=地址`
5. 保存文件

**注意**：钉钉 Webhook 地址格式为：
```
https://oapi.dingtalk.com/robot/send?access_token=xxxxxxxx
```

---

## 五、验证命令

### 5.1 验证配置加载

```powershell
cd C:\Users\zhuxiangbo\Desktop\project\analyse_information
python -c "from analyzer.pipeline import load_config; c = load_config(); print('OK')"
```

### 5.2 验证RSS源

```powershell
python -c "from analyzer.rss_fetcher import load_rss_feeds; feeds = load_rss_feeds(); print(f'RSS sources: {len(feeds)}')"
```

### 5.3 验证完整流程

```powershell
python analyzer/pipeline.py --url "https://www.kernel.org/doc/html/latest/driver-api/gpio/index.html"
```

### 5.4 验证金字塔模型

```powershell
python analyzer/pipeline.py --stats
```

---

## 六、最终数据流

```
TrendRadar（信号采集）
    ↓
n8n（自动化调度）
    ↓
analyse_information（AI分析 + 金字塔模型）
    ├→ Obsidian（知识沉淀，按10个分类归档）
    └→ 钉钉通知（分析结果）  ← 需要配置 DINGTALK_WEBHOOK
```

---

## 七、已完成验证的测试结果

| 测试项 | 结果 |
|--------|------|
| 配置加载 | ✅ 正常 |
| 模块导入 | ✅ 正常 |
| GPIO 文章分析 | ✅ 评分 10.0/10，分类: 08-设备驱动 |
| URL 去重 | ✅ 第二次跳过 |
| Token 统计 | ✅ 1959 tokens，¥0.0039 |
| 每日摘要 | ✅ 正常生成 |
| 全文搜索 | ✅ FTS5 正常 |
| 反馈机制 | ✅ useful/not_useful 正常 |

---

文档生成时间：2026-05-20
