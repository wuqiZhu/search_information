#### 方法 1：图形界面（最直观）

1. 按 `Win+R`，输入 `taskschd.msc` 回车，打开**任务计划程序**
2. 左侧点击 **任务计划程序库**
3. 中间列表往上滚动，直接找到 **TrendRadar**

#### 方法 2：PowerShell 直接打开属性（一键定位）

复制这条运行，直接调出配置窗口：

powershell

```
Get-ScheduledTask -TaskName "TrendRadar" | Show-ScheduledTask
```